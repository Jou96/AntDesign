import React from "react";

import { GroupOutlined, UserOutlined } from "@ant-design/icons";

export default function GroupOutlined() {
  return (
    <div>
      <GroupOutlined style={{ fontSize: "90px", color: "blue" }} spin={true} />
      <GroupOutlined style={{ fontSize: "90px", color: "blue" }} spin={true} />
    </div>
  );
}
